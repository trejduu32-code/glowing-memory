<h1><i class="fas fa-dashboard"></i> Dashboard</h1>
<p>Create and manage unlimited cron jobs</p>

<% if (unlimitedMode) { %>
<div class="unlimited-banner">
    <i class="fas fa-bolt"></i>
    <strong>UNLIMITED MODE ACTIVE</strong> - No rate limits, no restrictions
</div>
<% } %>

<div class="card">
    <h2><i class="fas fa-plus-circle"></i> Create New Cron Job</h2>
    <form action="/jobs" method="POST" class="job-form">
        <div class="form-group">
            <label for="name"><i class="fas fa-tag"></i> Job Name</label>
            <input type="text" id="name" name="name" required placeholder="e.g., API Health Check">
        </div>
        
        <div class="form-group">
            <label for="url"><i class="fas fa-link"></i> URL to Call</label>
            <input type="url" id="url" name="url" required placeholder="https://example.com/api/health">
        </div>
        
        <div class="form-group">
            <label for="schedule"><i class="fas fa-clock"></i> Cron Schedule</label>
            <input type="text" id="schedule" name="schedule" required 
                   placeholder="*/5 * * * *" 
                   title="Minute Hour Day Month DayOfWeek">
            <small class="help-text">Format: Minute Hour Day Month DayOfWeek (e.g., "*/5 * * * *" = every 5 minutes)</small>
        </div>
        
        <button type="submit" class="btn btn-primary">
            <i class="fas fa-plus"></i> Create Job
        </button>
    </form>
</div>

<div class="card">
    <h2><i class="fas fa-tasks"></i> Your Cron Jobs</h2>
    
    <% if (jobs.length === 0) { %>
        <div class="empty-state">
            <i class="fas fa-calendar-plus"></i>
            <h3>No cron jobs yet</h3>
            <p>Create your first cron job above!</p>
        </div>
    <% } else { %>
        <div class="jobs-table">
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>URL</th>
                        <th>Schedule</th>
                        <th>Status</th>
                        <th>Executions</th>
                        <th>Last Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% jobs.forEach(job => { %>
                        <tr>
                            <td><%= job.name %></td>
                            <td class="url-cell"><%= job.url %></td>
                            <td><code><%= job.schedule %></code></td>
                            <td>
                                <span class="status-badge <%= job.enabled ? 'status-active' : 'status-inactive' %>">
                                    <i class="fas <%= job.enabled ? 'fa-play' : 'fa-pause' %>"></i>
                                    <%= job.enabled ? 'Active' : 'Inactive' %>
                                </span>
                            </td>
                            <td><%= job.execution_count || 0 %></td>
                            <td>
                                <% if (job.last_status !== undefined && job.last_status !== null) { %>
                                    <span class="status-code <%= job.last_status >= 200 && job.last_status < 400 ? 'status-success' : 'status-error' %>">
                                        <%= job.last_status %>
                                    </span>
                                <% } else { %>
                                    <span class="status-na">N/A</span>
                                <% } %>
                            </td>
                            <td class="actions-cell">
                                <form action="/jobs/<%= job.id %>/toggle" method="POST" class="inline-form">
                                    <button type="submit" class="btn btn-sm <%= job.enabled ? 'btn-warning' : 'btn-success' %>">
                                        <i class="fas <%= job.enabled ? 'fa-pause' : 'fa-play' %>"></i>
                                        <%= job.enabled ? 'Pause' : 'Resume' %>
                                    </button>
                                </form>
                                
                                <a href="/jobs/<%= job.id %>/logs" class="btn btn-sm btn-info">
                                    <i class="fas fa-history"></i> Logs
                                </a>
                                
                                <form action="/jobs/<%= job.id %>/delete" method="POST" class="inline-form"
                                      onsubmit="return confirm('Are you sure you want to delete this job?');">
                                    <button type="submit" class="btn btn-sm btn-danger">
                                        <i class="fas fa-trash"></i> Delete
                                    </button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    <% } %>
</div>

<div class="card">
    <h3><i class="fas fa-graduation-cap"></i> Cron Schedule Examples</h3>
    <ul class="examples">
        <li><code>* * * * *</code> - Every minute</li>
        <li><code>*/5 * * * *</code> - Every 5 minutes</li>
        <li><code>*/30 * * * *</code> - Every 30 minutes</li>
        <li><code>0 * * * *</code> - Every hour</li>
        <li><code>0 */6 * * *</code> - Every 6 hours</li>
        <li><code>0 0 * * *</code> - Daily at midnight</li>
        <li><code>0 9 * * 1</code> - Every Monday at 9 AM</li>
        <li><code>*/10 * * * * *</code> - Every 10 seconds (6-field format)</li>
        <li><code>* * * * * *</code> - Every second (6-field format)</li>
    </ul>
</div>

<div class="card warning-card">
    <h3><i class="fas fa-exclamation-triangle"></i> Important Notes</h3>
    <ul class="warning-list">
        <li>This service runs in <strong>UNLIMITED MODE</strong> with no restrictions</li>
        <li>Jobs can run as frequently as every second</li>
        <li>Multiple instances of the same job can run concurrently</li>
        <li>Monitor your target URLs for their own rate limits</li>
        <li>Use responsibly - very frequent jobs may consume resources</li>
    </ul>
</div>